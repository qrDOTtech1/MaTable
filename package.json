{
  "name": "@atable/web",
  "private": true,
  "version": "0.1.0",
  "description": "A table ! — Dashboard restaurateur + page client commande par QR",
  "workspaces": ["apps/web"],
  "scripts": {
    "dev": "npm --workspace @atable/web run dev",
    "build": "npm --workspace @atable/web run build",
    "start": "npm --workspace @atable/web run start"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
